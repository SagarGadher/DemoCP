<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.sagar.democp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round_custom"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round_custom"
        android:supportsRtl="true"
        android:testOnly="false"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Pattern_Program_Detail_Activity" />
        <activity
            android:name=".Main_Activity"
            android:theme="@style/AppThemeCustom"
            android:screenOrientation="user"
            ></activity>
        <activity
            android:name=".Detail_Activity"
            android:configChanges="orientation"
            android:label="@string/title_activity_detail_" />
        <activity android:name=".Program_Detail_Activity" />
    </application>

</manifest>